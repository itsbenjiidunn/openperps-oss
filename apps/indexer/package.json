{
  "name": "@opp-oss/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist",
    "db:schema": "wrangler d1 execute openperps-indexer --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260701.1",
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.106.0"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4"
  }
}
